{
  "name": "ryanblock.com",
  "version": "1.0.0",
  "scripts": {
    "build": "./scripts/static-site-generator",
    "lint": "eslint . --fix",
    "start": "npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "^1.10.0",
    "clean-css": "^4.2.3",
    "eslint": "^7.0.0"
  }
}
