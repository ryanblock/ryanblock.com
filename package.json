{
  "name": "ryanblock.com",
  "version": "1.0.1",
  "scripts": {
    "build": "./scripts/static-site-generator",
    "lint": "eslint . --fix",
    "start": "PORT=4444 npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "^1.10.0",
    "clean-css": "^4.2.3",
    "eslint": "^7.0.0"
  }
}
